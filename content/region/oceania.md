---
title: "Oceania"
slug: "oceania"
region_key: "oceania"
region_name: "Oceania"
summary: "Oceania region hub (Australia/NZ and nearby) for routing stability and best-fit workloads."
description: "Oceania region hub."
page_type: "region"
---

## Overview

This page is a region hub for **Oceania**. It groups all data center pages that match:

- `region_key: oceania`
- OR `region` / `region_name` that normalizes to `oceania` (via `urlize` + `lower`)

## What you’ll find here

- **All data centers** in this region (sorted by `intent_score`).
- **China picks** (if you mark `china_pick: true` on datacenter pages).
- **Top providers** (detected from datacenter pages, and optionally linked to provider review pages).
